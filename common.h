#include<stdio.h>
#include<stdlib.h>
#include<stdbool.h>
//typedef enum boolean{FALSE,TRUE}Boolean;

void Error(char*);
void Warning(char*);

/*void Error(char *s)
{
	fprintf(stderr,"%s\n",s);
	exit(1);
}*/
